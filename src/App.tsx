// Arquivo que chama a tela principal (porta de entrada)
// App chama a tela principal

import Home from "./screens/Home";

export default function App(){
    return <Home/>; // Não precisa de colocar parênteses pois só temos uma linha, ou não temos lógica

}